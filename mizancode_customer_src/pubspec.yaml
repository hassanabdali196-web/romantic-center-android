name: mizancode_customer_rebuild
description: Rebuilt customer loyalty app with automatic account refresh.
publish_to: none
version: 1.4.0+14
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^6.0.2
  qr_flutter: ^4.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
