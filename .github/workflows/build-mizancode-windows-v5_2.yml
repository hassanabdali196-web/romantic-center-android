name: Build MizanCode Desktop v5.2 Invoice Sync

on:
  push:
    branches: [ "mizancode-flutter" ]
    paths:
      - '.github/workflows/build-mizancode-windows-v5_2.yml'
      - 'scripts/mizancode_v5_2_patch.py'
      - 'flutter_src/desktop_main.dart'
      - 'flutter_src/pubspec_desktop.yaml'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Create Windows project shell
        shell: pwsh
        run: |
          if (Test-Path builddesktop) { Remove-Item -Recurse -Force builddesktop }
          flutter create --org com.mizancode --project-name mizancode_desktop --platforms=windows builddesktop
          Copy-Item flutter_src/pubspec_desktop.yaml builddesktop/pubspec.yaml -Force
          Copy-Item flutter_src/desktop_main.dart builddesktop/lib/main.dart -Force

      - name: Inject v5.2 features
        run: python scripts/mizancode_v5_2_patch.py builddesktop/lib/main.dart

      - name: Apply MizanCode desktop name
        shell: pwsh
        run: |
          $p = 'builddesktop/windows/runner/main.cpp'
          $s = Get-Content $p -Raw
          $s = $s.Replace('L"mizancode_desktop"', 'L"MizanCode - ميزان كود"')
          Set-Content $p $s -Encoding UTF8

      - name: Get packages
        working-directory: builddesktop
        run: flutter pub get

      - name: Analyze app code
        working-directory: builddesktop
        run: flutter analyze lib --no-fatal-infos

      - name: Build Windows release
        working-directory: builddesktop
        run: flutter build windows --release

      - name: Package portable app
        shell: pwsh
        run: |
          $src = 'builddesktop/build/windows/x64/runner/Release'
          $dst = 'MizanCode_Desktop_v5.2_Invoice_Sync'
          if (Test-Path $dst) { Remove-Item -Recurse -Force $dst }
          Copy-Item -Recurse $src $dst
          'MizanCode Desktop v5.2 - invoice QR, printing, customer barcode selection and loyalty sync.' | Set-Content "$dst/README.txt" -Encoding UTF8
          Compress-Archive -Path "$dst/*" -DestinationPath 'MizanCode_Desktop_v5.2_Invoice_Sync.zip' -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: MizanCode-Desktop-v5.2-Invoice-Sync
          path: MizanCode_Desktop_v5.2_Invoice_Sync.zip
          retention-days: 30

      - name: Publish Windows release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: mizancode-desktop-v5.2-invoice-sync
          name: MizanCode Desktop v5.2 Invoice Sync
          prerelease: true
          files: MizanCode_Desktop_v5.2_Invoice_Sync.zip
